float clipToTex(float value) {
	return (value + 1.0) / 2.0;
}

float texToClip(float value) {
	return value * 2.0 - 1.0;
}

vec2 clipToTex(vec2 value) {
	return (value + 1.0) / 2.0;
}

vec2 texToClip(vec2 value) {
	return value * 2.0 - 1.0;
}

vec3 clipToTex(vec3 value) {
	return (value + 1.0) / 2.0;
}

vec3 texToClip(vec3 value) {
	return value * 2.0 - 1.0;
}

vec4 clipToTex(vec4 value) {
	return (value + 1.0) / 2.0;
}

vec4 texToClip(vec4 value) {
	return value * 2.0 - 1.0;
}
